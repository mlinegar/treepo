# Markov change-point oracle — flat TOML over MarkovChangepointConfig.
# Field defaults match upstream; only override what differs from canonical.
# The Markov auto-fixture is registered in _ORACLE_DOMAIN_FIXTURES, so
# example just forwards these knobs to `treepo.methods.run("oracle", ...)`.

[dgp]
# All fields default to MarkovChangepointConfig() upstream values; we set
# seed explicitly for reproducibility. To sweep the paper grid override
# n_regimes ∈ {3,5} × max_tokens ∈ {64,128} × seed ∈ {0,1,2}.
seed = 0
