"""Learned mergeable `g` experiments (CPU-only).

This subpackage provides a small torch-based framework for learning mergeable sketches
from *local* oracle supervision (leaf + internal-node labels), mirroring the TreePO
local-law view (C1/C3 + optional schedule consistency / idempotence regularizers).
"""

