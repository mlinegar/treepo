"""Runtime backbone for long-context evaluation and execution.

This package implements typed contracts + a minimal control loop used by
benchmark adapters (e.g., RULER) and evaluation scripts (e.g.,
`scripts/run_runtime_eval.py`).
"""

